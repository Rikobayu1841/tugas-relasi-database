<?php
$server = 'localhost';
$username ='root';
$password ='';
$database ='web';

$conn = new mysgli($server,$password,$database);

if($conn->connect_error)
{ die('Gagal Terhubung'.$conn->connect_error);
}
else
{
	echo '<h3>Berhasil Terhubung</h3>';
}
?>